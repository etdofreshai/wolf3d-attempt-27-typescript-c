import { describe, expect, it } from "vitest";

describe("save/load parity", () => {
  it("matches golden save/load fixtures", () => {
    expect.fail("not implemented yet — milestone M5 pending");
  });
});
