import { describe, expect, it } from "vitest";

describe("runtime parity", () => {
  it("matches golden runtime parity fixtures", () => {
    expect.fail("not implemented yet — milestone M7 pending");
  });
});
