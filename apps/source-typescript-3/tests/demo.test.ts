import { describe, expect, it } from "vitest";

describe("demo playback parity", () => {
  it("matches golden demo playback fixtures", () => {
    expect.fail("not implemented yet — milestone M6 pending");
  });
});
